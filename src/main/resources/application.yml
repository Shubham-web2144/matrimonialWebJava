server:
  port: 7000

Spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mwp
    username: root
    password: Shub@2144
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB